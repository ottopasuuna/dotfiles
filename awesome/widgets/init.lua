return {
    clock         = require("widgets.clock"),
    cpu           = require("widgets.cpu"),
    common        = require("widgets.common"),
    memory        = require("widgets.memory"),
    network       = require("widgets.network"),
    alsa          = require("widgets.alsa"),
    imap          = require("widgets.imap"),
    temperature   = require("widgets.temperature"),
    weather       = require("widgets.weather"),
    filesystem    = require("widgets.filesystem"),
    notifications = require("widgets.notifications")
}
